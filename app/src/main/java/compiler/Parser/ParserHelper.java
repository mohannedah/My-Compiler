package compiler.Parser;

public class ParserHelper {
    public static void expect(String expectation, String result) 
    {   
        // if(expectation != result) throw new ParseError(expectation, result);
    };
}
